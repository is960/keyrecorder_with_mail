After running "A-Start.exe" it will automatically control the operation of "Mail.exe" and "KeyRecord.exe".

After the program runs, it is hidden by default in the background.

If you want to end the program, open "kill.cmd" to automatically end all processes.